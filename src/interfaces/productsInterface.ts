export interface IProducts {
  id: string;
  name?: string;
  view_description?: string;
  description: string;
  price: string;
  location: string;
  stock: number;
  backgroundColor: string;
  icon: string;
}
